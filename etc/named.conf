/*
 * This is a worthless, nonrunnable example of a named.conf file that has
 * every conceivable syntax element in use.  We use it to test the parser.
 * It could also be used as a conceptual template for users of new features.
 */

/*
 * C-style comments are OK
 */

// So are C++-style comments

# So are shell-style comments

// watch out for ";" -- it's important!

options {
	directory "/var/named";			// use current directory
};

zone "opendarwin.org" {
        type master;                     // what used to be called "primary"
        file "opendarwin.org";
        allow-update { none; };
        allow-transfer { 204.152.188.37; 204.152.186.42; };              
        allow-query { any; };
        notify yes;                  // send NOTIFY messages for this
                                        // zone?  The global option is used
                                        // if "notify" is not specified
                                        // here.
        also-notify { };                // don't notify any nameservers other
                                        // than those on the NS list for this
                                        // zone
};

zone "synack.net" {
	type slave;
	file "synack.net";
	masters { 204.152.188.37; };
};

zone "notapple.org" {
	type slave;
	file "notapple.org";
	masters { 204.152.188.37; };
};

zone "darwinporting.org" {
        type master;                     // what used to be called "primary"
        file "darwinporting.org";
        allow-update { none; };
        allow-transfer { 204.152.188.37; };              
        allow-query { any; };
        // notify yes;                  // send NOTIFY messages for this
                                        // zone?  The global option is used
                                        // if "notify" is not specified
                                        // here.
        also-notify { };                // don't notify any nameservers other
                                        // than those on the NS list for this
                                        // zone
};

zone "darwinporting.info" {
        type master;                     // what used to be called "primary"
        file "darwinporting.info";
        allow-update { none; };
        allow-transfer { 204.152.188.37; };              
        allow-query { any; };
        // notify yes;                  // send NOTIFY messages for this
                                        // zone?  The global option is used
                                        // if "notify" is not specified
                                        // here.
        also-notify { };                // don't notify any nameservers other
                                        // than those on the NS list for this
                                        // zone
};

zone "metapkg.org" {
        type master;                     // what used to be called "primary"
        file "metapkg.org";
        allow-update { none; };
        allow-transfer { 204.152.188.37; };              
        allow-query { any; };
        // notify yes;                  // send NOTIFY messages for this
                                        // zone?  The global option is used
                                        // if "notify" is not specified
                                        // here.
        also-notify { };                // don't notify any nameservers other
                                        // than those on the NS list for this
                                        // zone
};

zone "metapkg.com" {
        type master;                     // what used to be called "primary"
        file "metapkg.com";
        allow-update { none; };
        allow-transfer { 204.152.188.37; };              
        allow-query { any; };
        // notify yes;                  // send NOTIFY messages for this
                                        // zone?  The global option is used
                                        // if "notify" is not specified
                                        // here.
        also-notify { };                // don't notify any nameservers other
                                        // than those on the NS list for this
                                        // zone
};

zone "xinetd.org" {
	type slave;
	file "xinetd.org";
	masters { 204.152.188.37; };
};

zone "bikemonkey.org" {
	type slave;
	file "bikemonkey.org";
	masters { 64.81.56.127; };
};

zone "hisrg.org" {
	type slave;
	file "hisrg.org";
	masters { 204.152.188.37; };
};

zone "darwinfo.org" {
	type slave;
	file "darwinfo.org";
	masters { 204.152.188.37; };
};

zone "darwinfo.net" {
	type slave;
	file "darwinfo.net";
	masters { 204.152.188.37; };
};

zone "darwinfo.com" {
	type slave;
	file "darwinfo.com";
	masters { 204.152.188.37; };
};

zone "localhost" IN {
	type master;
	file "localhost.zone";
	allow-update { none; };
};

zone "0.0.127.in-addr.arpa" IN {
	type master;
	file "named.local";
	allow-update { none; };
};

zone "." {
	type hint;			// used to be specified w/ "cache"
	file "cache.db";	
};

